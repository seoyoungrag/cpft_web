// @flow

import * as React from "react";

import { Error404Page } from "tabler-react";

type Props = {||};

function NotFound(props: Props): React.Node {
  return <Error404Page />;
}

export default NotFound;
